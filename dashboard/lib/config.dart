/// SHARED NETWORK CONFIG for Flutter dashboard.
///
/// After deploying to Render, paste your Render URL in [baseUrl].
/// Example: "https://pc-monitoring-backend.onrender.com"
class AppConfig {
  static const String baseUrl = 'https://spm-p86v.onrender.com';
  static const Duration refreshInterval = Duration(seconds: 5);
}
